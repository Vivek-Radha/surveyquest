{
  "name": "nextgen-survey",
  "version": "1.0.0",
  "description": "NextGen Survey System",
  "main": "index.js",
  "scripts": {
    "dev": "npx npm-run-all --parallel backend frontend",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
